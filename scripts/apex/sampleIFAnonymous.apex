String s='';

// s is blank --> string is blank
// s is NOT blank--> value of s.

if(string.isBlank(s)){
    System.debug(' String is BLANK. Come on');
} else {
    System.debug(s);
}


if(s== ''){

    System.debug('String is still BLANK. Come onnnnnn');
} else{

    system.debug(s);
}